
export interface Feature {
  id: string;
  title: string;
  description: string;
  icon: string;
}

export interface AppScreenshot {
  id: string;
  title: string;
  imageUrl: string;
}

export interface NavLink {
  label: string;
  href: string;
}
